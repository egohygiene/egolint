# Ansible inventory fixture

This directory contains a syntax-safe local inventory used to validate
Egolint's portable Ansible policy. It must not contain real hosts, credentials,
vault material, or organization-specific values.
